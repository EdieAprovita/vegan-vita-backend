# Base de Datos - Valores para CI/CD
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=vegan_vita_test

# JWT
JWT_SECRET=vegan-vita-super-secret-key-for-testing-only-min-32-chars
JWT_EXPIRES_IN=7d

# Node Environment
NODE_ENV=test

# SMTP (valores mock para testing)
SMTP_HOST=smtp.test.com
SMTP_PORT=587
SMTP_USER=test@test.com
SMTP_PASS=test-password
SMTP_FROM="VeganVita Test <noreply@test.com>"

# Frontend
FRONTEND_URL=http://localhost:3001
