name: 🚀 Deploy to Staging

on:
  push:
    branches: [development]
  workflow_dispatch:

env:
  NODE_VERSION: '20'
  PNPM_VERSION: '10'

jobs:
  deploy:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    env:
      STAGING_SSH_KEY: ${{ secrets.STAGING_SSH_KEY }}
      STAGING_SERVER_HOST: ${{ secrets.STAGING_SERVER_HOST }}
      STAGING_SERVER_USER: ${{ secrets.STAGING_SERVER_USER }}
      STAGING_APP_PATH: ${{ secrets.STAGING_APP_PATH }}
      STAGING_APP_URL: ${{ secrets.STAGING_APP_URL }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js and PNPM
        uses: ./.github/actions/setup-node-pnpm
        with:
          node-version: ${{ env.NODE_VERSION }}
          pnpm-version: ${{ env.PNPM_VERSION }}

      - name: Build application
        run: pnpm run build

      - name: Configure SSH
        if: env.STAGING_SSH_KEY != ''
        uses: webfactory/ssh-agent@v0.9.1
        with:
          ssh-private-key: ${{ env.STAGING_SSH_KEY }}

      - name: Add SSH known hosts
        if: env.STAGING_SSH_KEY != '' && env.STAGING_SERVER_HOST != ''
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ env.STAGING_SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to staging server
        if: env.STAGING_SSH_KEY != '' && env.STAGING_SERVER_HOST != '' && env.STAGING_SERVER_USER != ''
        run: |
          ssh ${{ env.STAGING_SERVER_USER }}@${{ env.STAGING_SERVER_HOST }} << 'EOF'
          cd ${{ env.STAGING_APP_PATH }}
          git pull origin development
          pnpm install --frozen-lockfile
          pnpm run build
          systemctl restart vegan-vita-backend-staging || true
          EOF

      - name: Health check
        if: env.STAGING_APP_URL != ''
        run: |
          sleep 5
          curl -f ${{ env.STAGING_APP_URL }}/api/health || exit 1

      - name: Deployment summary
        if: always()
        run: |
          echo "## 🚀 Staging Deployment" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** development" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
